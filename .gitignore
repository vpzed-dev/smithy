# local
.claude/settings.local.json
.venv/
notes.md
WORKFLOW.md
# opentofu
guards.tofu
variables.tofu
inventory/*.yaml
*.tfvars
*.tfvars.json
*.tfstate
*.tfstate.*
*.tfplan
*.tfplan.*
.terraform/
.terraform.lock.hcl.bak
# An OpenTofu/provider panic dumps a TRACE-level ring buffer here - full HTTP
# traffic including the live API token - regardless of TF_LOG.
crash.log
crash.*.log
# Live .env files; commit only the .env.example templates.
.env
# operator
prepush.env
fingerprints/**
!fingerprints/.gitkeep
# age/sops
.sops.yaml
keys.txt
*.agekey
secrets.enc.json
# Decrypted copies of secrets.enc.json (sops -d > ...).
*.dec.json
secrets.json
# otel-lgtm: reference/ is a scratch clone of the upstream otel-lgtm repo for
# research into the container's internals
/otel-lgtm/reference/
# Windmill
/windmill/local-dev/wmill.yaml
/windmill/local-dev/node_modules/
# logs/private
/logs/**
!/logs/.gitkeep
private/**
!private/.gitkeep
# scratch
tmp/
